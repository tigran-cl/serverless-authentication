{
  "name": "serverless-authentication",
  "version": "0.1.0",
  "main": "lib/index.js",
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "build-example": "npm run compile && cp -R lib example/authentication && cd example/authentication && sls function deploy"
  },
  "dependencies": {
    "lodash": "^4.6.0",
    "async": "^1.5.2",
    "jsonwebtoken": "^5.7.0",
    "serverless-helpers-js": "~0.1.0"
  }
}
