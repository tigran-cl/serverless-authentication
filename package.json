{
  "name": "serverless-authentication",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "private": false,
  "author": "Eetu Tuomala <eetu.tuomala@sc5.io>",
  "dependencies": {
    "async": "^1.5.2",
    "jsonwebtoken": "^5.7.0",
    "lodash": "^4.6.0",
    "request": "^2.69.0",
    "serverless-helpers-js": "~0.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "mocha": "^2.4.5",
    "sinon": "^1.17.3"
  },
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "build-example": "npm run compile && cp -R lib example/authentication",
    "deploy-example": "npm run build-example && cd example/authentication && sls function deploy",
    "test": "./node_modules/.bin/mocha spec/"
  }
}