{
  "name": "serverless-authentication",
  "version": "0.1.0",
  "main": "lib/index.js",
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "build-test": "npm run compile && node lib/index.js"
  },
  "dependencies": {
    "lodash": "^4.6.1"
  }
}
